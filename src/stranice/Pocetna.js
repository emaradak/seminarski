import React from 'react';
import PropTypes from 'prop-types';

const Pocetna = props => {
    return (
        <div>
            Pocetna
        </div>
    );
};

Pocetna.propTypes = {

};

export default Pocetna;